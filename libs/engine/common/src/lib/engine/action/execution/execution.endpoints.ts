import { ACTIONS_ENDPOINT } from '../action.endpoints';

export const ACTIONS_EXECUTIONS_ENDPOINT = `${ACTIONS_ENDPOINT}/executions`;

export const ACTION_PREVIEW_ENDPOINT = 'preview';

export const ACTION_CLEAN_ENDPOINT = 'clean';
